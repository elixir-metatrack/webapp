## Override the proxy targets for local development.
## Without these, dev proxy points at production (auth.metatrack.no / api.metatrack.no).
LOCAL_KEYCLOAK_URL=http://localhost:8180
LOCAL_API_URL=http://localhost:1234
